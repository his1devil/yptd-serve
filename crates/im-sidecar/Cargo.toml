[package]
name = "im-sidecar"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
description = "Rust client for the yptd OpenIM sidecar (NDJSON over a Unix socket)"

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
